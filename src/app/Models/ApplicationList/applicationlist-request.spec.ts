import { ApplicationlistRequest } from './applicationlist-request';

describe('ApplicationlistRequest', () => {
  it('should create an instance', () => {
    expect(new ApplicationlistRequest()).toBeTruthy();
  });
});
