import { JobApplicationFormRequest } from './job-application-form-request';

describe('JobApplicationFormRequest', () => {
  it('should create an instance', () => {
    expect(new JobApplicationFormRequest()).toBeTruthy();
  });
});
